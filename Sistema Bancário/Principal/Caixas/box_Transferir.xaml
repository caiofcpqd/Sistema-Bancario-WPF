<Window x:Class="Sistema_Bancário.Principal.Caixas.box_Transferir"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sistema_Bancário.Principal.Caixas"
        mc:Ignorable="d"
        Title="Transferir"
        Height="450"
        Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">

    <Window.Resources>

        <Style x:Key="InputTextBox"
               TargetType="TextBox">

            <Setter Property="Height"
                    Value="42"/>

            <Setter Property="FontSize"
                    Value="14"/>

            <Setter Property="Foreground"
                    Value="#333333"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="BorderBrush"
                    Value="#D5D5D5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="Padding"
                    Value="12,0"/>

            <Setter Property="VerticalContentAlignment"
                    Value="Center"/>

            <Setter Property="HorizontalContentAlignment"
                    Value="Left"/>

            <Setter Property="Template">

                <Setter.Value>

                    <ControlTemplate TargetType="TextBox">

                        <Border x:Name="InputBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">

                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"/>

                        </Border>

                        <ControlTemplate.Triggers>

                            <Trigger Property="IsMouseOver"
                                     Value="True">

                                <Setter TargetName="InputBorder"
                                        Property="BorderBrush"
                                        Value="#B9A0D8"/>

                            </Trigger>

                            <Trigger Property="IsFocused"
                                     Value="True">

                                <Setter TargetName="InputBorder"
                                        Property="BorderBrush"
                                        Value="#4B168C"/>

                                <Setter TargetName="InputBorder"
                                        Property="BorderThickness"
                                        Value="1.5"/>

                            </Trigger>

                            <Trigger Property="IsEnabled"
                                     Value="False">

                                <Setter TargetName="InputBorder"
                                        Property="Background"
                                        Value="#F2F2F2"/>

                                <Setter TargetName="InputBorder"
                                        Property="BorderBrush"
                                        Value="#DDDDDD"/>

                                <Setter Property="Foreground"
                                        Value="#999999"/>

                            </Trigger>

                        </ControlTemplate.Triggers>

                    </ControlTemplate>

                </Setter.Value>

            </Setter>

        </Style>

        <Style x:Key="CancelButton"
               TargetType="Button">

            <Setter Property="Width"
                    Value="105"/>

            <Setter Property="Height"
                    Value="38"/>

            <Setter Property="Background"
                    Value="White"/>

            <Setter Property="Foreground"
                    Value="#666666"/>

            <Setter Property="BorderBrush"
                    Value="#D5D5D5"/>

            <Setter Property="BorderThickness"
                    Value="1"/>

            <Setter Property="FontSize"
                    Value="13"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>

        <Style x:Key="TransferButton"
               TargetType="Button">

            <Setter Property="Width"
                    Value="105"/>

            <Setter Property="Height"
                    Value="38"/>

            <Setter Property="Background"
                    Value="#4B168C"/>

            <Setter Property="Foreground"
                    Value="White"/>

            <Setter Property="BorderThickness"
                    Value="0"/>

            <Setter Property="FontSize"
                    Value="13"/>

            <Setter Property="FontWeight"
                    Value="SemiBold"/>

            <Setter Property="Cursor"
                    Value="Hand"/>

        </Style>

    </Window.Resources>

    <Border Background="#F7F7F7"
            BorderBrush="#CFCFCF"
            BorderThickness="1">

        <Grid>

            <Grid.RowDefinitions>

                <RowDefinition Height="70"/>

                <RowDefinition Height="*"/>

                <RowDefinition Height="75"/>

            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="White"
                    BorderBrush="#E1E1E1"
                    BorderThickness="0,0,0,1">

                <Grid Margin="30,0">

                    <StackPanel VerticalAlignment="Center">

                        <TextBlock Text="Transferir dinheiro"
                                   FontSize="21"
                                   FontWeight="Bold"
                                   Foreground="#4B168C"/>

                        <TextBlock Text="Envie dinheiro para outra conta"
                                   FontSize="12"
                                   Foreground="#888888"
                                   Margin="0,3,0,0"/>

                    </StackPanel>

                    <Button Content="✕"
                            Width="32"
                            Height="32"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            Foreground="#777777"
                            BorderThickness="0"
                            FontSize="16"
                            Cursor="Hand"
                            Click="FecharButton_Click"/>

                </Grid>

            </Border>

            <Grid Grid.Row="1"
                  Margin="45,22,45,15">

                <Grid.RowDefinitions>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="Auto"/>

                    <RowDefinition Height="*"/>

                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Dados do destinatário"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           Margin="0,0,0,14"/>

                <TextBlock Grid.Row="1"
                           Text="CPF"
                           FontSize="12"
                           Foreground="#666666"
                           Margin="0,0,0,6"/>

                <TextBox Grid.Row="2"
                         x:Name="txtDestinatario"
                         Style="{StaticResource InputTextBox}"/>

                <TextBlock Grid.Row="3"
                           Text="Valor da transferência"
                           FontSize="12"
                           Foreground="#666666"
                           Margin="0,15,0,6"/>

                <TextBox Grid.Row="4"
                         x:Name="txtValor"
                         Style="{StaticResource InputTextBox}"/>

                <StackPanel Grid.Row="5"
                            Margin="0,15,0,0">

                    <TextBlock Text="Descrição (opcional)"
                               FontSize="12"
                               Foreground="#666666"
                               Margin="0,0,0,6"/>

                    <TextBox x:Name="txtDescricao"
                             Style="{StaticResource InputTextBox}"/>

                </StackPanel>

            </Grid>

            <Border Grid.Row="2"
                    Background="White"
                    BorderBrush="#E1E1E1"
                    BorderThickness="0,1,0,0">

                <Grid Margin="30,0">

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">

                        <Button Content="Cancelar"
                                Style="{StaticResource CancelButton}"
                                Margin="0,0,10,0"
                                Click="CancelarButton_Click"/>

                        <Button Content="Transferir"
                                Style="{StaticResource TransferButton}"
                                Click="TransferirButton_Click"/>

                    </StackPanel>

                </Grid>

            </Border>

        </Grid>

    </Border>

</Window>